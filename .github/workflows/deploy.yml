name: Deploy Theme Preview to GitHub Pages (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages-preview
  cancel-in-progress: true

jobs:
  deprecated:
    name: Deprecated (use validate-package-preview.yml)
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated."
          echo "Use .github/workflows/validate-package-preview.yml instead."
