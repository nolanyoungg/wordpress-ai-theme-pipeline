# Icons

This theme ships with inline/local SVG artwork in `assets/images/` to avoid external runtime dependencies.

If you add a dedicated icon set, store it here and keep all assets local (no CDN).

